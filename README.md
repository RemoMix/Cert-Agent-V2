
readme_content = '''# Cert-Print-Agent v3.0

نظام أتمتة معالجة شهادات المبيدات - Certificate Processing Automation System

## 📁 هيكل المجلدات / Folder Structure

```
Cert-Print-Agent/
│
├── config.yaml                          # إعدادات النظام
├── main.py                              # البرنامج الرئيسي
├── quick_start.py                       # تشغيل سريع
├── test_system.py                       # اختبار النظام
├── requirements.txt                     # المتطلبات
│
├── logs/                                # سجلات العمليات
│   └── processing.log
│
├── GetCertAgent/
│   ├── Source_Cert/                     # أصول الشهادات (أرشيف)
│   ├── Annotated_Certificates/          # الشهادات المكتوب عليها
│   ├── Printed_Annotated_Cert/          # الشهادات المطبوعة
│   ├── MyEmails/                        # البريد المحفوظ (.eml)
│   ├── Cert_Inbox/                      # الشهادات الواردة للمعالجة
│   ├── Processed/                       # مجلد المعالجة (قديم)
│   └── TempImages/                      # صور مؤقتة للـOCR
│
├── Agents/                              # وكلاء المعالجة
│   ├── OutlookAgent.py                  # مراقبة البريد
│   ├── ExtractLotAgent.py               # استخراج أرقام اللوت OCR
│   ├── ERPAgent.py                      # البحث في قاعدة البيانات
│   ├── AnnotatePrintAgent.py            # الكتابة والطباعة
│   └── LoggingAgent.py                  # التسجيل
│
├── Data/                                # البيانات
│   └── Raw_Warehouses.xlsx              # ملف ERP
│
└── Tesseract/ + Poppler/                # مكتبات OCR
```

## 🚀 التشغيل السريع / Quick Start

### 1. التثبيت / Installation

```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تثبيت Tesseract (Windows)
# حمل من: https://github.com/UB-Mannheim/tesseract/wiki
# ضع في مجلد Tesseract/

# تثبيت Poppler (Windows)
# حمل من: https://github.com/oschwartz10612/poppler-windows/releases
# ضع في مجلد Poppler/
```

### 2. الاختبار / Testing

```bash
# اختبار جميع المكونات
python test_system.py
```

### 3. التشغيل / Running

```bash
# تشغيل دورة واحدة فوراً
python quick_start.py

# أو مع مراقبة Outlook
python quick_start.py --with-outlook

# تشغيل مستمر (كل 5 دقائق)
python main.py

# تشغيل دورة واحدة
python main.py --once
```

## 📋 سير العمل / Workflow

```
┌─────────────────┐
│  Outlook Email  │
│  (Certificate)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Cert_Inbox/    │
│  (PDF/JPG)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│ ExtractLotAgent │────▶│   OCR (Tesseract)│
│                 │     └────────┬────────┘
│  - Lot Numbers  │              │
│  - Cert Number  │              ▼
│  - Product Name │     ┌─────────────────┐
└────────┬────────┘     │  Text Extraction│
         │              └─────────────────┘
         ▼
┌─────────────────┐
│   ERPAgent      │
│                 │
│  Search Excel:  │
│  - 2026 → 2023  │
│  - Supplier     │
│  - Internal Lot │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│AnnotatePrintAgent│
│                 │
│  1. Annotate PDF│
│  2. Print       │
│  3. Archive     │
└────────┬────────┘
         │
    ┌────┴────┬─────────────┐
    ▼         ▼             ▼
Source_Cert  Annotated_  Printed_
(Archive)   Certificates Annotated
```

## 🔧 أنماط أرقام اللوت / Lot Number Patterns

النظام يدعم الأنماط التالية:

| النمط | المثال | النوع |
|-------|--------|-------|
| Single | `139385` | لوت واحد |
| Explicit Multi | `139912/139913` أو `139912-139913` | لوتان صريحان |
| Implicit Multi | `139865/2` أو `139865/3` | لوت ضمني (+1, +2) |
| Alphanumeric | `SFP228` | رمز أبجدي رقمي |
| Dashed | `163-31-03-39-2394` | أرقام مفصولة |

## 📝 نصوص التذييل / Annotation Text

| الحالة | مثال |
|--------|------|
| لوت واحد | `باسم حنا - لوط 2601` |
| لوتان (نفس المورد) | `نصرى وسمين - لوط 2607 - لوط 2608` |
| لوت ضمني | `ماهر سعد - لوط 2605 +1` |
| موردين مختلفين | `باسم حنا: لوط 2601 \| نصرى وسمين: لوط 2607` |
| غير موجود | `غير مسجل في النظام / Not in ERP` |

## ⚙️ الإعدادات / Configuration

### `config.yaml`

```yaml
paths:
  base_dir: "Cert-Print-Agent"
  source_cert: "GetCertAgent/Source_Cert"
  annotated_cert: "GetCertAgent/Annotated_Certificates"
  printed_cert: "GetCertAgent/Printed_Annotated_Cert"
  cert_inbox: "GetCertAgent/Cert_Inbox"

printing:
  printer_name: "HP Neverstop Laser 100x PCLm-S"
  retry_attempts: 3
  annotation:
    position_x: 50
    position_y: 750  # أسفل الصفحة

ocr:
  language: "eng"
  dpi: 300
```

## 📊 ملف ERP / Excel Structure

**Raw_Warehouses.xlsx** يحتوي على 4 شيتات:
- `2026` - أحدث البيانات
- `2025`
- `2024`
- `2023`

**الأعمدة المطلوبة:**
- `NO` - رقم اللوت في الشهادة
- `Lot Num.` - رقم اللوت الداخلي
- `Supplier` - اسم المورد

## 🔍 استكشاف الأخطاء / Troubleshooting

### مشكلة: OCR لا يعمل
```bash
# تأكد من تثبيت Tesseract
tesseract --version

# تأكد من المسار في config.yaml
paths:
  tesseract_path: "Tesseract/tesseract.exe"
```

### مشكلة: لا يجد ملف Excel
```bash
# تأكد من وجود الملف
Cert-Print-Agent/Data/Raw_Warehouses.xlsx
```

### مشكلة: الطابعة غير متصلة
```bash
# النظام سيحفظ الملفات في Annotated_Certificates/
# ويعيد المحاولة تلقائياً
```

### مشكلة: Outlook لا يستجيب
- تأكد من فتح Outlook
- تأكد من صلاحيات الوصول
- جرب `outlook_autostart: true` في الإعدادات

## 📧 دعم اللغة العربية / Arabic Support

النظام يدعم:
- أسماء الموردين العربية في ERP
- كتابة النصوص العربية على PDF
- تسجيل السجلات بالعربية

**ملاحظة:** تأكد من وجود خط عربي في Windows:
- `C:/Windows/Fonts/arial.ttf`
- `C:/Windows/Fonts/tahoma.ttf`

## 📈 الإحصائيات / Statistics

النظام يسجل:
- عدد الشهادات المعالجة
- نسبة النجاح
- الأخطاء والمشاكل
- وقت المعالجة

**عرض السجلات:**
```bash
# آخر 50 سطر
Get-Content logs/processing.log -Tail 50

# البحث عن أخطاء
Select-String "ERROR" logs/processing.log
```

## 🛡️ الأمان / Security

- قاعدة بيانات SQLite لمنع المعالجة المكررة
- أرشيف لجميع الشهادات الأصلية
- نسخ احتياطي تلقائي

## 📞 الدعم / Support

للمشاكل أو الاقتراحات:
1. راجع سجلات الأخطاء في `logs/`
2. شغل `python test_system.py`
3. تأكد من إعدادات `config.yaml`

---

**الإصدار:** 3.0  
**آخر تحديث:** 2024
'''

with open('/mnt/kimi/output/README.md', 'w', encoding='utf-8') as f:
    f.write(readme_content)

print("✓ README.md تم إنشاؤه")
