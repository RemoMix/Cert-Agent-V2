Cert-Print-Agent


1- OutlookAgent: يراقب البريد → يحفظ الرسائل والمرفقات.

2- ExtractLotAgent:
يقرأ ملفات PDF/JPG من Cert_Inbox

يحول PDF إلى صور باستخدام poppler

يستخدم Tesseract OCR لاستخراج النص

يبحث عن أنماط أرقام اللوت باستخدام regex

يحلل بنية اللوت (واحد، متعدد، ضمني)

يسجل النتائج في ملف log
يقرأ الشهادة بـ OCR.

tesseract ملف موجود جوه المشروع
Cert-Print-Agent\Tesseract

يستخرج:

رقم/أرقام اللوت (Lot Number)

يحلل صيغة اللوت (واحد، متعدد، ضمني، إلخ).

3- ERPAgent:
يحمّل شيتات Excel الأربعة (2026-2023)

يبحث عن رقم اللوت في العمود الأول (NO)

يستخرج بيانات المورد (اسم المورد + رقم اللوت الداخلي)

يحلل نتائج البحث للعديد من اللوتات

ينشئ نص التذييل للطباعة

يتعامل مع الحالات الخاصة (لوط ضمني، موردين مختلفين، إلخ)

يسجل النتائج في ملف log
يبحث في Raw_Warehouses.xlsx (جميع الشيتات).

البحث بالتسلسل من 2026 إلى 2023 (الأحدث أولاً).

إذا وجد الرقم في شيت 2026 → يتوقف.

إذا لم يجده → يبحث في 2025، وهكذا.

يطابق رقم اللوت مع العمود الأول.

ملف Raw_Warehouses.xlsx:

4 شيتات: 2026، 2025، 2024، 2023

الأعمدة:

العمود الأول (A): NO → رقم اللوت في الشهادة (Cert Lot)

العمود الثالث (C): Lot Num → رقم اللوت الداخلي (External Lot)

العمود الرابع (D): Supplier → اسم المورد (بالعربية)

يستخرج:

اسم المورد (العمود 4)

رقم اللوت الداخلي (العمود 3)

4- AnnotatePrintAgent:

يكتب مباشرة نصًا على الشهادة (PDF):

اسم المورد (عربي)

رقم اللوت الداخلي

يحفظ الPDF المعدل

يتصل بالطابعة → يطبع.

اذا كانت الطبعة غير موصله يعيد المحاوله حتى تت الطباعة

printer_name = "HP Neverstop Laser 100x"

5- LoggingAgent: يسجل كل خطوة.
# سيسجل:
- وقت بدء المعالجة
- رقم الشهادة المعالج
- أرقام اللوت المستخرجة
- نتيجة البحث في ERP
- حالة الطباعة
- أي أخطاء



1. 📧 OutlookAgent
   ↓
2. 📄 ExtractLotAgent (OCR)
   ↓
3. 🔍 ERPAgent (يبحث في Excel)
   ↓
4. ✏️ AnnotatePrintAgent (يكتب و يحفظ و يطبع)
   ↓
5. 📝 LoggingAgent (يسجل كل شيء)